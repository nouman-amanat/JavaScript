// Data Types in JavaScript
// use Strict Mode         treat all javascript as strict mode this is new methode in new version 

const SomeText= "Hello World";
const SomeNumber= 123;
const SomeBoolean= true;
const SomeArray= [1,2,3,4,5];
const SomeObject= {name: "John", age: 30, city: "New York"};
console 
console.log(typeof(SomeText), typeof(SomeNumber), typeof(SomeBoolean), typeof(SomeArray), typeof(SomeObject));










